import 'package:flutter/material.dart';

/// Central palette
/// ColorScheme is seeded from these
class LoquiColors {
  const LoquiColors._();

  static const seed = Color(0xff5b6cff);
  static const surfaceDark = Color(0xff16171b);
  static const surfaceLight = Color(0xfff7f7fa);
}
